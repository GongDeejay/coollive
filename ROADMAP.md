# 功能路线图

本文档记录已规划但尚未开发的功能需求，作为后续开发的输入。

---

## 随手记模块 — 图片附件

### 需求描述

条目编辑区增加「配图」按钮，支持三种图片来源：

| 来源 | 说明 | 实现路径 |
|------|------|---------|
| **系统相册** | 调用 `<input type="file" accept="image/*">` | 纯前端，最简单 |
| **网络搜索图** | 输入关键词，展示可用图片（如 Unsplash API / Pexels API） | 需要免费图片 API |
| **AI 生图** | 输入描述，调用图片生成 API | 需要选定图生图服务（Stability AI / 小米 mimo-v2 如有 / 其他） |

### 数据结构扩展

在条目 JSON 中新增 `images` 字段（不破坏现有 localStorage 结构）：

```json
{
  "id": "uuid",
  "images": [
    {
      "id": "img-uuid",
      "type": "upload | unsplash | ai",
      "url": "https://... 或 data:image/...",
      "caption": "可选描述",
      "width": 800,
      "height": 600
    }
  ]
}
```

### 依赖决策（开发前需确认）

1. AI 生图用什么 API？（成本、质量、速度）
2. 网络搜索图用 Unsplash（需注册 API Key）还是其他？
3. 图片是存服务器还是只存 URL？（存服务器需要对象存储）

---

## 演示模块 — 全面升级

### 现状

当前演示功能：勾选条目 → 侧边面板排序 → 下载 HTML。
问题：单页仅有文字，无图文搭配；缺少封面/结束页/目录；无演示库管理。

### 目标

做到「可以独立演讲的演示稿」，类比轻量版 PPT，但保持设计风格一致。

---

### 功能模块 A：演示编排界面升级

**从侧边面板升级为全屏编排模式**

```
┌──────────────────────────────────────────────────────────┐
│ 演示编辑器                                    [预览] [导出] │
├────────────┬────────────────────────────────────────────┤
│ 幻灯片列表  │                 预览区域                     │
│            │                                            │
│ [封面]     │   ┌─────────────────────────────────┐      │
│ [目录]     │   │                                 │      │
│ ─────────  │   │    当前幻灯片内容预览              │      │
│ [条目 1]   │   │                                 │      │
│ [条目 2]   │   └─────────────────────────────────┘      │
│ ─────────  │                                            │
│ [+插入]    │   类型: [金句▼]  图片: [上传] [搜索] [AI]   │
│ [结束页]   │   文字: [编辑内容...]                       │
└────────────┴────────────────────────────────────────────┘
```

**可插入的页面类型**

| 类型 | 用途 |
|------|------|
| 封面页 | 标题 + 副标题 + 日期，自动置首 |
| 目录页 | 自动列出所有条目标题，带序号 |
| 金句页 | 一句话 + 来源日期（现有） |
| 内容页 | 场景/感受/体会三行（现有） |
| 关键词页 | 关键词列表（现有） |
| 图文页 | **新增** 图片 + 文字并排或上下排版 |
| 分隔页 | 空白过渡页，带章节标题 |
| 结束页 | 感谢/联系方式，自动置末 |

**页面顺序管理**

- 拖拽排序（`drag` API）
- 上移/下移按钮
- 封面/结束页锁定在首尾

---

### 功能模块 B：幻灯片排版升级

**图文页排版选项**

```
布局 A: 左图右文（图占40%，文占60%）
布局 B: 上图下文（图占50%高度）
布局 C: 背景图 + 叠加文字（需图片较暗或加遮罩）
布局 D: 纯图（图片全屏，适合配图金句）
```

**HTML 生成改进**

- 打印样式优化（每页一张，letter 或 16:9 比例）
- 过渡动画选项：无/淡入/滑入（CSS 动画，无 JS 依赖）
- 移动端适配（手机投屏场景）

---

### 功能模块 C：演示库管理

**需求：** 保存已生成的演示稿，不再只是下载一次性 HTML。

**数据结构**

```json
{
  "deck_id": "uuid",
  "title": "...",
  "created_at": "...",
  "updated_at": "...",
  "is_public": false,
  "slides": [...],
  "html_cache": "生成的 HTML 字符串（可选缓存）"
}
```

**存储方案**

- 未登录：localStorage（与随手记条目一起）
- 已登录：云端 API `/deck/` 接口（参考日记云端同步模式）

**演示库 UI**

- 在 App.jsx 导航加「演示」tab
- 列表显示：标题 + 页数 + 创建时间
- 操作：编辑、复制链接（已公开）、下载 HTML、删除

---

### 开发阶段规划

| 阶段 | 内容 | 前提条件 |
|------|------|---------|
| **Phase 1** | 图片附件（仅系统上传） | 确定图片存储方案 |
| **Phase 2** | 演示编排全屏模式 + 封面/结束/分隔页 | Phase 1 完成 |
| **Phase 3** | 图文页排版 + AI 生图 | 确定图生图 API |
| **Phase 4** | 演示库管理（本地 + 云端） | Phase 2/3 完成 |
| **Phase 5** | 公开演示分享链接 | Phase 4 完成 |

---

## 其他已规划功能

### 线索（Thread）系统

参见 `PHILOSOPHY.md` — 把相关随手记条目串联成一个持续推进的主题。
- 条目打 `thread` 标签
- 线索页面聚合同一主题的所有条目
- 时间线视图

### ZenTalk 对话 ↔ 随手记打通

- 对话结束后一键存为随手记条目
- 随手记条目关联对话 session（`zen_session_id` 字段已预留）
- 对话时带入近期随手记作为背景上下文

### 3D 测验 → ZenTalk 打通

参见 `api-contracts.mdc` — 测验结果通过 HTTP 调用 `/api/chat` 接入。
`src/lib/integrations/conversation-adapter.ts` 接口已定义，待实现。

---

*最后更新：2026-05*  
*有新需求请先在此文档添加规划，讨论后再开发*
